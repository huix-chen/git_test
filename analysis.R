# Example R script
data <- c(1, 2, 3, 4, 5)
mean_value <- mean(data)
print(mean_value)


logistic = function(x, y) {
  # For example, you can calculate the logistic function
  result = 1 / (1 + exp(-(x + y)))
  # Return the result
  return(result)
}


